class AliveCheck {
    public static String checkAlive(String name) {
        if (name == "Yashas") {
            return name + " is alive";
        } else if (name == "Alice") {
            return name + " is alive";
        } else if (name == "Bob") {
            return name + " is alive";
        } else if (name == "Charlie") {
            return name + " is alive";
        } else if (name == "David") {
            return name + " not alive";
        } else if (name == "Eve") {
            return name + " is alive";
        } else if (name == "Frank") {
            return name + " not alive";
        } else if (name == "Grace") {
            return name + " is alive";
        } else if (name == "Hank") {
            return name + " not alive";
        } else if (name == "Ivy") {
            return name + " is alive";
        }
            return name + " is not found";
    }
}